#ifndef STRUTIL_H
#define STRUTIL_H

/* Reverse a string in place. Returns the string pointer. */
char *str_reverse(char *s);

/* Count occurrences of character c in string s. */
int str_count_char(const char *s, char c);

#endif
